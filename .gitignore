# Claude Code — configuración personal de cada desarrollador
.claude/

# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.swp
*.swo

# Logs y temporales
*.log
*.tmp
*.bak

# Dependencias (por si en el futuro se añade Node)
node_modules/
.env
.env.local

# Carpeta de pruebas locales y archivos de test temporales
tests/__pycache__/

# Documentos comerciales / propuestas (datos personales del proveedor — no van al repo)
propuesta-*.html
propuesta-*.pdf
cotizacion-*.html
cotizacion-*.pdf

# Fotos originales pesadas + sus versiones comprimidas — van a Cloudinary, no al repo.
# Los scripts python de la carpeta sí son útiles, pero los conservamos solo localmente
# para no inflar el repo. Si se necesitan reusar, viven en esta carpeta de scratch.
fotoscomprimir/
